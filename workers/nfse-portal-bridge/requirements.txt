# Worker Python: Postgres + HTTP ao portal (HMAC).
# Python 3.11+ recomendado (paridade com NFSE_dist).
psycopg[binary]>=3.2
pytest>=8.0
filelock>=3.16
python-dotenv>=1.0
certifi>=2024.0.0
# Win32 APIs no worker Windows: monitoriza/clica diálogos nativos de selecção de
# certificado quando o Chromium for Testing não respeita a policy AutoSelect.
pywin32>=306; sys_platform == "win32"
