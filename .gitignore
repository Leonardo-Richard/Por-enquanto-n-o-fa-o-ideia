

# Environment & Secrets (AIOS)
.env
.env.local
.env.*.local
*.key
*.pem

# NFSE_dist upstream (obtido com npm run vendor:nfse-dist)
third_party/NFSE_dist/
third_party/NFSE_dist-main/
third_party/nfse-dist-main.zip

# Worker / certificado ADN — nunca versionar segredos reais (CE-NFR2)
clients.local.json
**/*.pfx
certificates/*.pfx

# Logs do worker NFSE portal bridge (scripts/windows/start-nfse-portal-bridge.ps1)
workers/nfse-portal-bridge/logs/

# Lock do motor ADN browser (filelock) — runtime local
.adn_browser_worker.lock

# Dependencies (AIOS)
node_modules/
node_modules

# Build & Logs (AIOS)
dist/
build/
.next/
.turbo/
*.log
logs/

# IDE & OS (AIOS)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOS Local (AIOS)
.aios-core/local/
.claude/settings.local.json
.aios/install-log.txt
