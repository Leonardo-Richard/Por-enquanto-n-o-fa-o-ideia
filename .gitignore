

# Environment & Secrets (AIOS)
.env
.env.local
.env.*.local
*.key
*.pem

# Worker / certificado ADN — nunca versionar segredos reais (CE-NFR2)
clients.local.json
**/*.pfx
certificates/*.pfx

# Dependencies (AIOS)
node_modules/
node_modules

# Build & Logs (AIOS)
dist/
build/
.next/
.turbo/
*.log
logs/

# IDE & OS (AIOS)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOS Local (AIOS)
.aios-core/local/
.claude/settings.local.json
.aios/install-log.txt
